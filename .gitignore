node_modules
.next
out
dist
tsconfig.tsbuildinfo
# .env is committed on purpose (non-secret defaults; see the file header).
# Anything secret goes in .env.local, which stays ignored.
.env.local
.env.production
data/
.run/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
