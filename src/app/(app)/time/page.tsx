import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function TimePage() {
  return (
    <PlaceholderScreen
      eyebrow="Marketing Bull / Tracking"
      title="Time"
      phase="Phase 3 — next up"
      description="Log hours against a client or project, with the billing rate resolved once at save (project override → client rate) and frozen on the entry, so editing a rate never restates history. The 98 time entries from mission-control import over the same rails the clients and projects used."
      meanwhile="Hours by Project on the dashboard reads ClickUp time tracking."
    />
  );
}
