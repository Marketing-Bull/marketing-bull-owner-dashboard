import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function CalendarPage() {
  return (
    <PlaceholderScreen
      eyebrow="Marketing Bull / Tracking"
      title="Calendar"
      phase="Phase 5"
      description="Month, week, and work-week views over Google Calendar — view-only against the primary calendar, confirmed safe now that the auto-scheduler is dormant and targets its own calendar anyway."
      meanwhile="the dashboard's Calendar widget shows today plus the next two days, with the event modal."
    />
  );
}
